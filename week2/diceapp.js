let diceSize = process.argv[2];                               // get number of sides on a dice


if(diceSize.charAt(0) !== "D"){
    console.log("Choose your dice size by ")

}

let numSides = diceSize.substring(1);

